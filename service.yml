apiVersion: v1
kind: Service
metadata:
  namespace: solar-system
  labels:
    app: solar-system
    name: solar-system-service
spec:
  selector:
    app: solar-system
  ports:
  - port: 80 
    protocol: TCP
    targetPort: 80
  type: NodePort

